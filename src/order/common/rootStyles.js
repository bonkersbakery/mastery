export const styles = theme => ({
  root: {
    flexGrow: 1,
    paddingTop: 50
  },
  paper: {
    width: "98%"
  },
  paperContainer: {
    paddingTop: theme.spacing.unit * 9,
    paddingBottom: theme.spacing.unit * 2
  }
});
